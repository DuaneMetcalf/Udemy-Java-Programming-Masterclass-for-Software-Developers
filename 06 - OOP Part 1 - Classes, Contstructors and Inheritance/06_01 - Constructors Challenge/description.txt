Create a new class for a bank account
Create fiels for the account number, balance, customer name, email and phone number.

Create geetters and setters for each field
Create two additional methods
1. To allow the customer to deposit funds (This should increment the balance field)
2. To allow the customer to withdraw funds (This should deduct from the balance field, but now allow the withdrawl to complete if there are insufficient funds)

Create a new class VipCustomer
It should have 3 fields, name, credit limit and email address.
Create 3 constructors.
1st constructor empty and should call the constructor with 3 parameters with default values.
2nd contstructor should pass on the 2 values it receives and and a default value for the 3rd.
3rd constructor should have all the fields.
Create getters only and setters won't be needed